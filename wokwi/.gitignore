# Configuration personnelle — chacun garde la sienne, jamais committée.
src/config_locale.h

# Dossier de build PlatformIO — régénéré par `pio run`, pas versionné.
.pio/

# Journal de localtunnel — recréé à chaque lancement, jamais versionné.
localtunnel-log.txt